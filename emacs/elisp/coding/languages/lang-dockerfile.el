;;; lang-dockerfile.el ---  -*- lexical-binding: t; -*-
;;
;;; Commentary:
;; Emacs dockerfile configuration
;;
;;; Code:

;; Mode
(use-package
  dockerfile-mode
  :ensure t)

(provide 'lang-yaml)
;;; lang-dockerfile.el ends here
