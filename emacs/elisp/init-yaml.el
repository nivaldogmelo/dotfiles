;;; init-yaml.el ---  -*- lexical-binding: t; -*-
;;
;;; Commentary:
;; Emacs yaml configuration
;;
;;; Code:


;; YamlMode
(use-package
  yaml-mode
  :ensure t)
;; -YamlMode


(provide 'init-yaml)
;;; init-yaml.el ends here
